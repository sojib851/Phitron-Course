#include<bits/stdc++.h>
using namespace std;

int main()
{
    int x, y;
    cin >> x >> y;
    if (x == y)
    {
        cout << "X Is Equal to Y : " << x << endl;
    }
    else if (y > x)
    {
        cout << "X <- " << এর চেয়ে Y বড়ো : " << endl;
    }
    
    


    return 0;
}